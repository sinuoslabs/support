import { Collect } from './collect';

describe('Collect', () => {
  let collect = new Collect();

  beforeEach(async () => {
    //
  });

  it('should be defined', () => {
    expect(collect).toBeDefined();
  });
});
